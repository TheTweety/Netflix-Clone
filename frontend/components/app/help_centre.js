import { connect } from "react-redux";
import HelpCenter  from "./help"; 



const msp = (state, ownProps) => ({
    
})

const mdp = dispatch => ({
        
})

export default connect(msp, mdp)(HelpCenter);