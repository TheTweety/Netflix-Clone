import { connect } from "react-redux";   
import PricingGuestContainer from "./pricing_guest"
const msp = state => ({
     
})

const mdp = dispatch => ({ 
})

export default connect(msp, mdp)(PricingGuestContainer);