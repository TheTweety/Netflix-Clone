import { connect } from "react-redux"
import NavGuest from "./nav_guest"; 

const mapStateToProps = state => ({   
})

const mapDispatchToProps = dispatch => ({  
})

export default connect(mapStateToProps, mapDispatchToProps)(NavGuest);